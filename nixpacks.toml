[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/backend run build"]

[start]
cmd = "node --enable-source-maps ./backend/dist/index.mjs"
